package controller;

import org.junit.Test;

import view.ImageGUIView;

import static org.junit.Assert.assertEquals;

/**
 * Tests the stuff related to our Features Controller.
 */
public class ControllerFeaturesTest {

  // Tests that our controller can accept a view.
//  @Test
//  public void testGivingAView() {
//    StringBuilder str = new StringBuilder();
//    ImageGUIView mock = new MockGUI(str);
//    ImageFeatures feat = new ControllerFeatures("New Control", mock);
//    assertEquals(str.toString(), "Hey we did it");
//  }
}