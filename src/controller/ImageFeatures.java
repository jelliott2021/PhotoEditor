package controller;

import model.IImage;

/**
 * Represents a controller that can be used with a GUI to process
 * commands.
 */
public interface ImageFeatures {

  /**
   * Executes controller
   */
  void execute();
}
